<UserControl x:Class="FtpHelper.ViewMode.FileState"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
     >

    <Grid>
        <DataGrid ColumnWidth="*" AutoGenerateColumns="False" x:Name="dataGrid">
            <DataGrid.Columns>
                <DataGridTextColumn Header="文件名" Binding="{Binding FileName}"/>
                <DataGridTextColumn Header="文件路径" Binding="{Binding FilePath}"/>
                <DataGridCheckBoxColumn Header="状态" Binding="{Binding FileState}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
